function toggleActiveClass(nameOfClass,cssClass){const acc=document.querySelectorAll(nameOfClass);for(let i=0;i<acc.length;i+=1){acc[i].addEventListener('click',function(){this.classList.toggle(cssClass);const panel=this.nextElementSibling;if(panel.style.maxHeight){panel.style.maxHeight=null;}else{panel.style.maxHeight=`${panel.scrollHeight}px`;}});}}
toggleActiveClass('.menu__accordion-button','active');document.querySelectorAll('.accordion').forEach((item)=>{item.addEventListener('click',(event)=>{document.querySelectorAll('.accordion-content').forEach((contentElement)=>{const content=contentElement;if(content!==event.target.nextElementSibling){content.style.maxHeight=null;content.parentElement.classList.remove('opened');}});const content=event.target.nextElementSibling;if(content.style.maxHeight){content.style.maxHeight=null;content.parentElement.classList.remove('opened');}else{content.style.maxHeight=`${content.scrollHeight}px`;content.parentElement.classList.add('opened');}});});