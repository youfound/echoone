const body=document.querySelector('body');const hambBtn=document.querySelector('#hamb-btn');const hambMenu=document.querySelector('#hamb-menu');hambBtn.addEventListener('click',function(){hambBtn.classList.toggle('is-active');hambMenu.classList.toggle('is-menu-open');body.classList.toggle('noscroll');});const header=document.querySelector('header');window.addEventListener('scroll',(e)=>{const topValue=body.getBoundingClientRect().top;if(Math.abs(topValue)>0){header.classList.add('header--fixed');}else{header.classList.remove('header--fixed');}});window.addEventListener('touchmove',(e)=>{const topValue=body.getBoundingClientRect().top;if(Math.abs(topValue)>0){header.classList.add('header--fixed');}else{header.classList.remove('header--fixed');}});